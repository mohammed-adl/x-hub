-typing done // not working for isnant sending messages right after first send, need to wait 0.25 sec or so, but we will fix this after we get it life
-pagination perfect
-live messages perfect
--messages from profile perfect --except that starting a new conversation will not trigger a new conversation on other side (it has to be existing one or user has to mount) we still fixing this after we go live
-- emojis done
--file uploads still not implemented
-ui and respnsivness perfect // probelm at typing
-- seen indicators still not implemented

speed // very big issue, but its from app itself